# M4 Firecracker readiness map

Status date: 2026-08-10. This map records implementation and evidence status;
it is not a Firecracker isolation or release claim.

| Requirement | Locally proved control/refusal | Remaining code gap | External evidence blocker |
| --- | --- | --- | --- |
| SBX-021 / DEP-007 / TST-005 | Fail-closed Linux/amd64/KVM preflight, deny-all plan compiler, pinned-artifact validator, Jailer resource/cleanup contract, signed M3/M4 preparation handoff. | Real staged-host implementation, launch-started/terminal-observation/recovery chain, final fixture lock. | Protected Linux/amd64 runner with usable `/dev/kvm`, Jailer, cgroups v2, reviewed fixtures, retained guest boot and cleanup evidence. |
| SBX-022 | `NewLocalUnsafeClient` requires the literal `local-unsafe` acknowledgement, accepts only caller-supplied sanitized convenience environment, advertises every authority profile as unavailable, and refuses secret, mount, volume and isolation requests. | No further local-adapter code gap. Its in-memory control fixture deliberately does not execute processes or prove durability. | None for the acknowledgement/refusal contract; it must never satisfy security evidence. |
| SBX-023 | `FakeControlClient` uses a manually advanced UTC `FakeClock` and scripts accepted/queued/started/terminal state, typed failure, process result and explicit operation gap records without command execution, network or filesystem access. | No further fake-control code gap. It is deterministic test evidence only, not process or restart durability evidence. | None. |
| SBX-024 | `Client.Capabilities` returns a fully structured, versioned profile whose digest is derived from every advertised profile fact. Create and restore operations bind that digest; reconnect refuses a changed/regressed profile. | Capability precision and architecture claims remain unavailable until each real profile has evidence. | Firecracker states remain unavailable until their own suites run. |
| SBX-025 / TST-003 | One black-box capability-contract suite runs against both `local-unsafe` and deterministic fake clients in the normal Go gate, requiring a derived snapshot and fail-closed unsupported isolation request. | Add a Firecracker control client to this same suite only when its Linux/KVM profile is able to advertise a capability. | Linux/KVM adapter run once it advertises a capability. |
| SBX-026--027 | Public control admission now requires canonical non-reserved guest paths, exact immutable copy-in metadata, declared copy-out media type, and bounded transfer options. The private worker streams verified regular files through a descriptor-rooted `/workspace` view, stages atomic replacement, applies safe mode/owner metadata, and returns only an immutable copy-out reference. No adapter advertises transfer. | Connect the worker to the durable host execution path; add directory/archive, cancellation/partial-cleanup observation, Linux replacement/mount-crossing race coverage, and S9/S10 transfer conformance. | None for the worker unit/integration path; Firecracker transfer still needs data-plane and protected Linux/KVM proof. |
| SBX-028 | Firecracker compiler rejects host mounts and reports mount capability unavailable. | Jailed sharing daemon and complete traversal/TOCTOU/special-file conformance suite. | Linux/KVM daemon-boundary tests. |
| SBX-029--032 | Private resource authority now atomically persists principal-owned volume manifests, immutable tombstones, exclusive generation-fenced attachments, expired-lease reconciliation, descriptor-first mount identity leases, taint provenance, and bounded AES-256-GCM snapshot staging/publish/open/delete with plaintext/ciphertext integrity checks and taint-attestation denial. Restore requires the exact live snapshot-lease generation and equal sandbox/effective-spec/capability/image ceiling before exposing a verified plaintext reader to an injected sink. Firecracker volume/snapshot/mount descriptors remain unavailable. | Connect this local durable store to authenticated control operations and the Firecracker host/guest execution path; add archive, crash/restart reconciliation, and a real snapshot-exclusion canary. | Linux/KVM crash, corruption, deletion-race, sharing-daemon and artifact-canary evidence. |
| SBX-033--036 | Request-level grant validation and chunk redaction now pair with an internal command-secret lifecycle owner: contextual finite resolver request, explicit ephemeral-sink/audit seams, delivery reservation, safe audit facts, literal-redaction bytes, sink-confirmed tree-reap revocation and retained-byte zeroization. A secret-free authenticated command authorization is exact-bound to the host envelope tuple. Its `SecretExecutionAuthority` now scopes that resolver/audit lifecycle to one authenticated same-vsock exchange: the guest must echo one canonical contextual request before resolution; the host sends at most 16 KiB only over that connection; a sealed guest runner must acknowledge delivery before start; output is held, redacted, and sent to the durable host emitter only after tree-reap plus a revocation acknowledgement. Lost post-start contact zeroizes the host copy and records `lost-contact-reaper-required`; it never claims revocation. The static fixture refuses this operation before requesting any secret, because it lacks the required protected verifier. The Linux guest `SecretSink` writes only a sealed anonymous `memfd`, passes a read-only duplicate only as fixed `exec.Cmd.ExtraFiles` descriptor 3, rejects ambient inherited descriptors, observes the inherited `/proc/<pid>/fd/3` and `TracerPid`, holds a `pidfd`, and refuses revocation until both leader exit and an injected cgroup-aware complete-tree verifier confirm reaping. Its secret runner consumes only that descriptor path, aborts a failed prestart launch, and refuses uncertain post-start cleanup. It retains no Go copy of delivery bytes; closing the sealed memfd after manager redaction-copy zeroization releases the ephemeral kernel object. LinuxJailerHost/HostProcessExecutor expose this only as a profile-gated, currently unavailable composition door. No secret profile is advertised. | Compose the sealed guest runner and cgroup-aware tree verifier through a protected Jailer profile; implement a non-snapshotted secret area policy and actual ptrace/proc/mount isolation. Implement an external typed credential broker or admitted immutable trusted-exec verification for elevated credentials. The current `TracerPid` check is an observation/refusal, not proof that a later ptrace or `/proc` disclosure is impossible. | Protected Linux/amd64 KVM same-vsock/FD/proc/ptrace/daemon, cgroup tree-reap, no-snapshot canary, hostile workdir/environment, cancellation/lost-ack reaper, and broker/trusted-exec evidence before enabling secrets. |
| SBX-037--038 | Direct IP and malformed grants remain rejected; foundation has no NIC and egress is unavailable. A frozen per-command egress lease now matches only admitted domain/transport/port rules, expires finitely, resolves in the proxy boundary, and dials only one public resolved address using the reviewed special-use denylist. The private guest transport now has one bounded `PROXY` exchange: an exact control-signed envelope carries one lease/VM/fence-bound DNS request and input; the guest must echo that exact canonical request; the host rechecks the authority, performs host-owned resolution/dialing, returns at most one bounded output, and closes the session on terminal, cancellation deadline, or Jailer reaping. It exposes no listener, guest resolver, literal address, socket handle, or generic tunnel. | Compose lease issuance, resolver/dialer ownership, durable output acknowledgement and cancellation from the authenticated control/host path into this transport. The actual Firecracker topology must keep the guest NIC-less (or otherwise prove no default/direct DNS/metadata/DoH/private-control route) while exposing only the fixed authenticated AF_VSOCK endpoint; test DNS CNAME/IDNA/IPv6 pinning and quota accounting against the real proxy. | Protected Linux/amd64 KVM evidence of the exact no-NIC/no-route Jailer topology, AF_VSOCK peer/auth lifecycle, metadata/DoH/bypass denial, resolver lifecycle, proxy outage, cancellation, reaper cleanup, and retained data-plane evidence. |
| SBX-039 | Unsupported profiles remain unavailable. | Release gate aggregating all profile evidence. | Every required profile suite and its retained evidence. |

The local `cmd/firecracker-e2e` capability report currently refuses execution on
Darwin because it is not Linux, has no `/dev/kvm` or cgroups v2, and has no
reviewed final fixture plan. Do not replace this block with a simulated pass.
