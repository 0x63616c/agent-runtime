# M7 acceptance matrix

This matrix is the M7 owner’s evidence plan. The binding requirements remain
the master requirements and acceptance ledger; this file records only the
approved seams, demonstrated coverage, and the next missing proof. A row stays
unclaimed until `evidence/requirements-ledger.json` contains a SHA-bound,
passing evidence entry of the required scope.

| Requirement | Approved seam | Demonstrated implementation coverage | Required acceptance proof still needed | Claim status |
| --- | --- | --- | --- | --- |
| TOL-001 | S5 Tool broker | Atomic broker admission, normalized model handoff, tool worker finalization. | Durable PostgreSQL/MinIO broker-to-grant-to-result proof retained at `evidence/m7-tool-broker-durable-postgres-minio.json` (code revision `168513d8b30e5476d09c7e1d7abd587e563aee1f`). | directly proven |
| TOL-002 | S5 Tool broker | Builtin wrapper, sandbox adapter, and Streamable HTTP MCP adapter implement the operation-ID recovery contract. | Retained unit contract suite at `evidence/m7-tol002-brokered-adapter-contract.json` (code revision `4945dfc23991f2a35261567b08fdcb598bcf0314`). The sandbox adapter is a declared control-client seam only, not an EX-002 or protected-sandbox claim. | directly proven |
| TOL-003 | S5 Tool broker | Disposable PostgreSQL/MinIO integration covers single-use exhaustion plus expiry, revocation, cancellation, denial, and unavailable-policy terminal refusal without adapter dispatch. | Retained at `evidence/m7-tol003-grant-terminal-postgres-minio.json` (code revision `4945dfc23991f2a35261567b08fdcb598bcf0314`). The fixture adapter is not a Workspace service or protected sandbox. | directly proven |
| TOL-004 | S5 Tool broker | The disposable PostgreSQL/MinIO lifecycle test reads separately authorized audit and outbox pages and checks their linked order across pending, allow, deny, expiry, revocation, cancellation, exhaustion, and result. Tool audit facts retain only actor/tenant, agent and policy revision commitments, capability-scope digest, tool-call/operation correlation, and bounded timestamps; an owner scope cannot enumerate either private page. | Retained at `evidence/m7-tol004-audit-decision-postgres-minio.json` (code revision `4945dfc23991f2a35261567b08fdcb598bcf0314`). This is authorized runtime/audit-page integration, not an HTTP or sandbox claim. | directly proven |
| TOL-005 | S5 Tool broker | Disposable PostgreSQL/MinIO integration retains a successful bounded redacted Artifact/public projection and fails an oversized result without a new Artifact/object or raw public value. | Retained at `evidence/m7-tol005-output-limits-postgres-minio.json` (code revision `4945dfc23991f2a35261567b08fdcb598bcf0314`). The fixture adapter is not a protected sandbox. | directly proven |
| TOL-006 | S5 Tool broker | MCP cancellation and status-only reconciliation return uncertainty rather than re-submit an effect; the disposable PostgreSQL/MinIO proof records an initial Worker-supplied operation ID, loses that worker before outcome/acknowledgement, then reopens the runtime store and reconciles only that ID into a safe uncertain public terminal state with no Artifact. | Retained at `evidence/m7-tool-uncertain-recovery-postgres-minio.json` (code revision `7b97ffc1f1595b65168c0010062237b8ad333200`). | directly proven |
| HITL-001 | S5/S6 | The Session workflow observes a claimed durable input route while its Turn is `waiting_for_approval`, continues at the expected boundary, and starts no Invocation; the disposable public API-process check reads the pending Approval and waiting Turn before and after restart. | Retained at `evidence/m7-hitl001-pending-workflow-postgres-minio.json` (code revision `c991c188e08e13f94bf96214674a7abf2f8db718`); Broker seeding remains private setup and no model/sandbox claim is made. | directly proven |
| HITL-002 | S6 public approval | The closed public Approval schema requires stable approval/session/turn/tool linkage plus requester, policy, safe action, bounded scope, expiry, and state; durable public process reads retain that safe projection across restart. | Retained at `evidence/m7-hitl002-approval-golden-contract-postgres-minio.json` (code revision `c991c188e08e13f94bf96214674a7abf2f8db718`); legacy snapshots without an original safe action fail closed rather than inventing one. | directly proven |
| HITL-003 | S6 public approval | The public HTTP/SDK fake-clock matrix verifies owner authorization, cross-principal safe not-found, approval replay, denial, scope-change refusal, expiry, and no Tool dispatch. | Retained at `evidence/m7-hitl003-public-authorization-fake-clock.json` (code revision `c991c188e08e13f94bf96214674a7abf2f8db718`). | directly proven |
| HITL-004 | S5/S6 | The disposable PostgreSQL/MinIO lifecycle proof records public safe Product events plus independently authorized audit/outbox records for explicit approval, denial, injected-clock expiry, pending cancellation, and post-approval tool finalization. Each semantic event route has the exact audit-fact/operation correlation in persisted order; private publisher routing is closed for every terminal approval event. | Retained at `evidence/m7-approval-event-audit-postgres-minio.json` (code revision `c3174ec16fe1132b91a6c649c8a1156f7e6f4081`). | directly proven |
| HITL-005 | S6 public approval | Retained local Stack proof covers pending reset/reconnect/idempotent replay and a separate public short-expiry late-decision conflict with no execution. | Retained at `evidence/m7-workspace-approval-public-stack-e2e.json` (code revision `cb6d9905c5b0da7c4ff483ad8dd4ac10e9fcc40b`). | directly proven |
| HITL-006 | S1 Workspace Agent | Retained disposable PostgreSQL/MinIO API-process proof runs both shipped binaries: web approve/replay/cancel/expiry, terminal deny, non-owner inbox isolation, and browser reconnect after API restart. | Retained at `evidence/m7-workspace-public-client-postgres-minio.json` (code revision `e916a7e2e8297332986e83274d3b34495390601e`); broker seeding and no-sandbox scope remain explicit. | directly proven |
| EX-002 | S1 plus M4 S9 | A declared local fixture produces an owner Artifact after approval, explicitly without a sandbox. | Session-scoped protected Workspace sandbox, safe file APIs, live process output, and Linux/KVM profile evidence. | blocked on M4 |

## Scope boundary

The declared local fixture is usable only for application composition evidence.
It does not execute a Workspace service, Sandbox, Firecracker, or protected
profile. M4 retains the Linux/KVM, Jailer, portable-transfer, mount, volume,
secret, and egress profile evidence required before EX-002 can be claimed.
