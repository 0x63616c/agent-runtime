# M5 readiness audit

Status: implementation audit; incomplete. This is not a requirement promotion
or a substitute for the one M5 independent review.

## Ownership result

The authoritative terminal-owner map is
[work-map.md](work-map.md): issue #23 owns `DOM-001` through `DOM-013` and
`API-001` through `API-012`; issue #24 owns `DAT-001` through `DAT-013`; and
issue #25 owns `TMP-002` through `TMP-004` plus `TMP-009` and `TMP-010`.
The generated requirements catalog records every one of those rows as M5. M6
owns only `MOD-*`; M7 owns only `TOL-*`, `HITL-*`, and its examples. A later
milestone may depend on the M5 foundations, but it does not transfer their
terminal evidence ownership.

## Implemented and tested foundation

| Area | Current implementation and evidence | Remaining terminal gap |
| --- | --- | --- |
| DOM-001–011 (bounded foundation) | Immutable Agent revisions, revision-pinned Sessions, bounded Input references, serialized Turns, invocation records/fences, cancellation and settlement are implemented by `internal/runtimestate`; `compiler_planner_test.go`, `state_runtime_test.go`, and durable API integration cover the initial lifecycle. `AppendConversation` persists only immutable content references under expected-version conflict/idempotency, while `RegisterArtifact` and the state-authorized content reader provide ID/digest metadata plus principal-checked readback. `StateRuntime.SendInput` now accepts an Artifact reference only after state authorizes the caller's exact ID, digest, media type, and size; the disposable PostgreSQL/MinIO test recompiles the runtime composition and proves valid admission plus forged/cross-principal denial. An approved Approval creates a bounded capability grant, and worker consumption enforces owner, policy revision, expiry, and maximum uses before an external effect. `internal/runtimetool` reads only the state-authorized immutable sandbox-control descriptor, refuses missing/corrupt/cross-scope descriptors before execution, and reconciles a lost claim by the exact operation ID without resubmission. Its terminal observation persists a correlated `sandbox_operation.finalized` product event/outbox route, which the Temporal publisher and durable dispatcher recheck. The durable PostgreSQL/MinIO lifecycle now executes that path through the concrete authenticated TLS sandbox-control adapter and reaches a terminal control-plane ledger result; it is not external sandbox-command execution evidence. `internal/runtimemodel` owns a provider-neutral invocation adapter: fresh intents invoke once, expired claims reconcile by the same operation ID, and both persist a fenced terminal result plus optional unknown-preserving token usage before acknowledgement. Its concrete `HTTPAdapter` validates a bounded normalized NDJSON stream and finalizes successful output as an owner-readable immutable Artifact; `TestDurableModelNormalizedStreamFinalizesOwnerReadableOutput` runs that adapter through disposable PostgreSQL/MinIO, then proves public Turn output metadata, Artifact readback, and terminal replay. The local HTTP endpoint is a protocol fixture, not a live provider claim. | A deployed model-role composition with an approved supported provider/credential source remains absent. External sandbox-command execution evidence remains absent. |
| API-001–012 (bounded public/admin foundation) | Temporal-free SDK/OpenAPI route contract, strict HTTP handling, bounded public models, authorization, cursors, cancellation, inspection and model-profile allowlist have unit/architecture coverage. Artifact download is a state-authorized SDK/HTTP read; scoped durable receipt status is observable without replaying work. Approval inspect and idempotent approve/deny are public Go SDK and HTTP operations with an owner-scoped StateRuntime integration test and HTTP/SDK contract test. A fake-clock StateRuntime regression test proves a late decision durably transitions the Approval to `expired`, records the terminal audit fact, and returns a safe conflict instead of authorizing execution. The same real StateRuntime behind the HTTP handler and public Go SDK proves the safe conflict, durable expired projection, idempotent retry, and retained receipt for an expired Approval decision. A short-retention, real HTTP/SDK scenario proves an expired Input mutation is not replayed, status lookup returns safe conflict, and a fresh idempotency key admits a distinct Turn. Tenant administrators can create, revise, replay, and read immutable named Policy revisions through the compiler/planner/state-store/HTTP/SDK boundary; direct StateRuntime composition also refuses catalog mutations without `Identity.Admin`. The combined HTTP/SDK matrix distinguishes catalog administration from owner-scoped data lifecycle: it covers Agent and Policy revisions, Artifact reads, Approval inspection/decision, receipts, Input, Session/Turn/Tool inspection, Events, cancellation, and close. Same-tenant non-admin and cross-tenant callers receive only safe failures with no private content or bearer value. The disposable PostgreSQL/MinIO HTTP process proves a Policy remains readable by its administrator and non-enumerable to a member after restart. A checked-in v1 OpenAPI compatibility baseline preserves public routes, success statuses, schema fields, and existing enum members; its release-consumer compile test uses a separate temporary module with `GOWORK=off` and imports retained plus additive public SDK capabilities. `docs/reference/api-compatibility.md` gives the compatible-addition versus breaking-change migration policy. | No additional API-011/API-012 implementation gap is recorded. The candidate-module check is not evidence of a published immutable release tag. |
| DAT-001–013 (durable foundation and operational boundaries) | Conversation expected-version/idempotent planner behavior, immutable Artifact digest authorization, planner-produced ordered events/audit/outbox, CAS state persistence, cursor behavior, and lease claim/ack are tested. State records use named independently configurable retention classes, with a linted inventory for every runtime-state class. Runtime v5 moves the active PostgreSQL snapshot and retention-job authority to native tenant hash partitions and forced RLS. Each store transaction sets its exact `tenant_id` locally; the disposable database matrix proves an unbound app role sees no state, a bound role cannot read a second tenant, and the narrow operator projection can enumerate partitions. An operator-authorized PostgreSQL retention collector locks one tenant, deletes only expired unpinned Agent/Input/Artifact/Conversation metadata and exact no-longer-referenced content, and records its next due collection; content failure leaves metadata intact. The PostgreSQL/MinIO harness proves safe collection, state-authorized artifact input, bounded artifact streams, and tenant erasure isolation. Every successful planner mutation atomically commits redacted attempted/authorized/committed audit phases, each with an exact durable audit-export route; closed outcome and publisher commands add terminal/reconciled phases. Audit-route bookkeeping deliberately does not recursively audit itself. The real PostgreSQL/HTTP outage test retains a claimed route with no `published` fact, then after lease expiry exports the exact retained fact and records publication. Audit retention exceeds its outbox route. The codec role has an optional bounded HTTPS sink with no credential and at-least-once non-fail-closed delivery. A PostgreSQL/MinIO/Temporal child-process matrix proves durable claim and post-Temporal/pre-ack kill recovery. The disposable integration runs all migrations, RLS/partition proof, rollback refusal, safe erasure/cross-tenant denial, and a custom-format backup/restore drill that recovers an exact tenant row into a fresh database. `runtime-operations-drill` plus its manually dispatched protected workflow now automate the exact role-grant/scheduled-retention, configured sink outage/recovery/retention, and source-WAL/isolated-target verification; it refuses missing authority and writes a schema-validated redacted report only after an actual protected run. | No retained successful `agent-runtime.operations-evidence/v1` artifact exists. The exact remaining external condition is one approved protected `runtime-operations` environment run with current source/isolated-restore DSNs, separate authorized retention/PITR tenant records, active WAL archive, and configured audit-sink drill endpoints; until that artifact is uploaded, no live production role-grant, retention, audit-sink, or PITR claim is made. |
| TMP-002–004, TMP-009–010 (Session route) | Private Temporal-free public API boundary; owned codec factory; durable state/outbox scheduler; replay-safe Session workflow versioning, duplicate tolerance, Continue-As-New, and development-server replay evidence. `TestSessionWorkflowExercisesEveryOwnedTerminalEffectRoute` runs the test-environment matrix for approval/sandbox finalization, normalized-model success, producer-gap terminal failure, explicit cancellation, and close/drain. `TestSessionWorkflowRetriesOnlyRetryableDurableDispatch` proves real Temporal retry is limited to durable-route observation after state unavailability; `TestSessionWorkflowDoesNotRetryCancelledDurableDispatch` proves typed cancellation has no replacement attempt; `TestDispatchStateCommandClassifiesRetrySafetyWithoutRepeatingUnknownEffects` covers deterministic, uncertain, and incompatible cases. The owned Temporal development-server test records the actual Input → Approval → sandbox-finalization → Session-complete lifecycle; `internal/runtimeorchestration/testdata/session-workflow-v1-history.json` retains its serialized history and bounded payload blobs, which CI replays through the registered private workflow. The disposable PostgreSQL/MinIO tool integration persists the exact staged descriptor, approval/grant, execution result, and correlated finalization Event, then reads the durable replay projection back. A separate PostgreSQL/Temporal test loses the durable acknowledgement only after Temporal accepts the exact input route, recreates the publisher client, reclaims the lease, and records `published` with no second state dispatch; the child-process matrix covers before-route and after-route kill boundaries. | No additional code-owned TMP-009/TMP-010 gap is recorded. This is state/control-plane proof only: it does not claim a live provider or external sandbox command. |

## Missing M5 work before review

The acceptance ledger specifies the missing proof/behavior, not merely
documentation:

- DOM-008–009, DOM-012–013: durable grant expiry/max-use and safe public
  Approval/Turn outcome evidence now exist, including concrete TLS
  sandbox-control adapter recovery, unknown-preserving public model usage, and
  bounded owner-scoped Tool intent/execution inspection. Production
  normalized stream finalization and durable owner readback now exist. Deployed
  model-provider composition remains absent.
- DAT-009–013: native tenant partitions/RLS, narrow database capability groups,
  due-retention metadata, safe erasure/collection, full planner audit phase
  routes, durable outbox recovery, optional bounded HTTPS audit sink, disposable
  backup/restore, and fail-closed protected operational-drill automation are
  implemented. The sole remaining external condition is a retained successful
  protected-environment report using current authorized role-grant, retention,
  audit-sink, WAL, and isolated-PITR capabilities. The disposable tenant-erasure
  and publisher process-kill matrices remain bounded composition evidence; they
  do not promote those production controls.
- TMP-009–010: the retry/effect matrix is now explicitly tested and mapped:
  retryable state observation, typed cancellation, deterministic/uncertain
  refusal, approval/sandbox and model terminal routes, PostgreSQL/Temporal
  acknowledgement recovery, process-kill recovery, and retained replay. The
  durable approval/grant/tool lifecycle crosses the concrete HTTPS
  sandbox-control adapter before recording its finalization event. This remains
  a control-plane adapter proof, not external sandbox execution; similarly the
  normalized model HTTP fixture is not a live provider claim.

The existing M5 implementation must not claim these rows or request its
independent review until this table is closed with named evidence.
