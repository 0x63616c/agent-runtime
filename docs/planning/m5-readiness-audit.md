# M5 readiness audit

Status: implementation audit; incomplete. This is not a requirement promotion
or a substitute for the one M5 independent review.

## Ownership result

The authoritative terminal-owner map is
[work-map.md](work-map.md): issue #23 owns `DOM-001` through `DOM-013` and
`API-001` through `API-012`; issue #24 owns `DAT-001` through `DAT-013`; and
issue #25 owns `TMP-002` through `TMP-004` plus `TMP-009` and `TMP-010`.
The generated requirements catalog records every one of those rows as M5. M6
owns only `MOD-*`; M7 owns only `TOL-*`, `HITL-*`, and its examples. A later
milestone may depend on the M5 foundations, but it does not transfer their
terminal evidence ownership.

## Implemented and tested foundation

| Area | Current implementation and evidence | Remaining terminal gap |
| --- | --- | --- |
| DOM-001–011 (bounded foundation) | Immutable Agent revisions, revision-pinned Sessions, bounded Input references, serialized Turns, invocation records/fences, cancellation and settlement are implemented by `internal/runtimestate`; `compiler_planner_test.go`, `state_runtime_test.go`, and durable API integration cover the initial lifecycle. `AppendConversation` persists only immutable content references under expected-version conflict/idempotency, while `RegisterArtifact` and the state-authorized content reader provide ID/digest metadata plus principal-checked readback. `StateRuntime.SendInput` now accepts an Artifact reference only after state authorizes the caller's exact ID, digest, media type, and size; the disposable PostgreSQL/MinIO test recompiles the runtime composition and proves valid admission plus forged/cross-principal denial. An approved Approval creates a bounded capability grant, and worker consumption enforces owner, policy revision, expiry, and maximum uses before an external effect. `internal/runtimetool` reads only the state-authorized immutable sandbox-control descriptor, refuses missing/corrupt/cross-scope descriptors before execution, and reconciles a lost claim by the exact operation ID without resubmission. Its terminal observation persists a correlated `sandbox_operation.finalized` product event/outbox route, which the Temporal publisher and durable dispatcher recheck. The durable PostgreSQL/MinIO lifecycle now executes that path through the concrete authenticated TLS sandbox-control adapter and reaches a terminal control-plane ledger result; it is not external sandbox-command execution evidence. `internal/runtimemodel` owns a provider-neutral invocation adapter: fresh intents invoke once, expired claims reconcile by the same operation ID, and both persist a fenced terminal result plus optional unknown-preserving token usage before acknowledgement. Public HTTP/SDK `InspectTurn` returns only that latest provider-neutral usage and the stable safe Failure contract; omitted usage remains unknown. | Conversation/model production-provider composition and normalized streaming remain absent. A public Tool-intent/execution inspection surface and external sandbox execution evidence remain absent. |
| API-001–012 (bounded public/admin foundation) | Temporal-free SDK/OpenAPI route contract, strict HTTP handling, bounded public models, authorization, cursors, cancellation, inspection and model-profile allowlist have unit/architecture coverage. Artifact download is a state-authorized SDK/HTTP read; scoped durable receipt status is observable without replaying work. Approval inspect and idempotent approve/deny are public Go SDK and HTTP operations with an owner-scoped StateRuntime integration test and HTTP/SDK contract test. A fake-clock StateRuntime regression test proves a late decision durably transitions the Approval to `expired`, records the terminal audit fact, and returns a safe conflict instead of authorizing execution. The same real StateRuntime behind the HTTP handler and public Go SDK proves the safe conflict, durable expired projection, idempotent retry, and retained receipt for an expired Approval decision. A short-retention, real HTTP/SDK scenario proves an expired Input mutation is not replayed, status lookup returns safe conflict, and a fresh idempotency key admits a distinct Turn. Tenant administrators can create, revise, replay, and read immutable named Policy revisions through the compiler/planner/state-store/HTTP/SDK boundary; direct StateRuntime composition also refuses catalog mutations without `Identity.Admin`. The combined HTTP/SDK matrix distinguishes catalog administration from owner-scoped data lifecycle: it covers Agent and Policy revisions, Artifact reads, Approval inspection/decision, receipts, Input, Session/Turn/Tool inspection, Events, cancellation, and close. Same-tenant non-admin and cross-tenant callers receive only safe failures with no private content or bearer value. The disposable PostgreSQL/MinIO HTTP process proves a Policy remains readable by its administrator and non-enumerable to a member after restart. A checked-in v1 OpenAPI compatibility baseline preserves public routes, success statuses, schema fields, and existing enum members; its release-consumer compile test uses a separate temporary module with `GOWORK=off` and imports retained plus additive public SDK capabilities. `docs/reference/api-compatibility.md` gives the compatible-addition versus breaking-change migration policy. | No additional API-011/API-012 implementation gap is recorded. The candidate-module check is not evidence of a published immutable release tag. |
| DAT-001–013 (durable foundation and operational boundaries) | Conversation expected-version/idempotent planner behavior, immutable Artifact digest authorization, planner-produced ordered events/audit/outbox, CAS state persistence, cursor behavior, and lease claim/ack are tested. State records use named independently configurable retention classes, with a linted inventory for every runtime-state class. The PostgreSQL authority partitions application state by canonical `tenant_id`: every transition/lifecycle operation takes that tenant's advisory lock and atomically updates its one authoritative snapshot row; immutable bytes remain outside PostgreSQL. An operator-authorized PostgreSQL retention collector locks one tenant, deletes only expired unpinned Agent/Input/Artifact/Conversation metadata and exact no-longer-referenced content, and leaves metadata intact on content failure. The disposable PostgreSQL/MinIO test proves denied collection preserves an exact staged Artifact, while authorized collection deletes its expired metadata and MinIO object but preserves the live Session/Input. The same disposable stack proves only a state-authorized exact immutable Artifact reference survives fresh composition; forged/cross-principal references are safe `not_found`. `StateRuntime.OpenArtifact` repeats authorization before a bounded stream; the HTTP/SDK path exposes only media, size, and digest and refuses missing/mismatched/short/overlong streams. The PostgreSQL/MinIO harness proves tenant erasure deletes only target metadata and exact referenced objects while another tenant stays readable. An exact audit fact/export route commits with each transition. The real PostgreSQL/HTTP outage test retains a claimed route with no `published` fact, then after lease expiry exports the exact retained fact and records publication. Audit retention exceeds its outbox route in that proof. The `orchestration-codec` role now has an optional, bounded HTTPS audit-sink configuration; it contains no credential and preserves at-least-once, non-fail-closed delivery. A PostgreSQL/MinIO/Temporal child-process matrix terminates the publisher after durable claim and after Temporal accepts the route but before acknowledgement; rebuilt publication reclaims the route and records `published` only after dispatch. The producer-loss/model path retains explicit uncertain/gap/terminal facts and public cursor replay. The disposable PostgreSQL integration runs migrations, rollback refusal, state-store conflicts, safe erasure/cross-tenant denial, and a custom-format backup/restore drill that recovers an exact tenant row into a fresh database. | The full named attempted/authorized/committed/terminal/reconciled audit lifecycle is still not proven for every M5 operation. No reference Stack enables an operational audit sink, native PostgreSQL partition maintenance, RLS/least-privilege database roles, a production retention job, or WAL archive/recovery target. Production audit-sink retention/outage evidence and a live authorized PITR drill therefore remain external production-run blockers. |
| TMP-002–004 (Session route) | Private Temporal-free public API boundary; owned codec factory; durable state/outbox scheduler; replay-safe Session workflow versioning, duplicate tolerance, Continue-As-New, and development-server replay evidence. The owned Temporal development-server test records the actual Input → Approval → sandbox-finalization → Session-complete lifecycle; `internal/runtimeorchestration/testdata/session-workflow-v1-history.json` retains its serialized history and bounded payload blobs, which CI replays through the registered private workflow. The disposable PostgreSQL/MinIO tool integration persists the exact staged descriptor, approval/grant, execution result, and correlated finalization Event, then reads the durable replay projection back. A separate PostgreSQL/Temporal test loses the durable acknowledgement only after Temporal accepts the exact input route, recreates the publisher client, reclaims the lease, and records `published` with no second state dispatch. | Full M5 behavior and release evidence remain incomplete; the retained-history and composed PostgreSQL-to-Temporal lifecycle gaps are closed. |

## Missing M5 work before review

The acceptance ledger specifies the missing proof/behavior, not merely
documentation:

- DOM-008–009, DOM-012–013: durable grant expiry/max-use and safe public
  Approval/Turn outcome evidence now exist, including concrete TLS
  sandbox-control adapter recovery, unknown-preserving public model usage, and
  bounded owner-scoped Tool intent/execution inspection. Production
  model-provider composition and normalized streaming remain absent.
- DAT-009–013: the tenant-scoped PostgreSQL snapshot/lock authority, safe
  erasure/collection, durable outbox recovery, optional bounded HTTPS audit
  sink configuration, and disposable backup/restore drill are implemented.
  Still missing: the complete named audit lifecycle over every M5 operation,
  live audit-sink retention/outage evidence, native partition maintenance/RLS,
  production retention execution, and an authorized WAL-backed PITR drill.
  The disposable tenant-erasure and publisher process-kill matrices remain
  bounded composition evidence; they do not promote those production controls.
- TMP-010: the retained historic replay and composed PostgreSQL-to-Temporal
  lost-ack/restart evidence are now present. The durable approval/grant/tool
  lifecycle additionally crosses the concrete HTTPS sandbox-control adapter
  before it records the finalization event. This remains a control-plane
  adapter proof, not external sandbox execution. The remaining requirement-
  level evaluation still belongs to the final M5 audit. TMP-009 has a closed
  retry decision table and focused classification proof, but remains part of
  the final M5 evidence bundle.

The existing M5 implementation must not claim these rows or request its
independent review until this table is closed with named evidence.
