---
title: Local foundation
description: What can honestly be run during M0.
---

# Local foundation

The Go foundation gate and the documentation site are currently runnable. Use
the repository's `just check` gate for Go foundation changes. Use `just docs`
to install the locked documentation dependencies and start the local site.

The intended local product environment is Tilt on macOS, but `tilt up` is not
an implemented instruction yet. It will only appear here after the declarative
stack, lifecycle, and cleanup tests exist.
